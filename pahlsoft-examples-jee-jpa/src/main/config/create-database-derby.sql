CREATE TABLE events
(
  eventID SMALLINT PRIMARY KEY NOT NULL GENERATED BY DEFAULT AS IDENTITY ( START WITH 1, INCREMENT BY 1),
  title CLOB NOT NULL,
  description CLOB NOT NULL,
  status VARCHAR(45) NOT NULL
)

